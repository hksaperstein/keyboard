# _config.yml

title: "Leyboards and Keyboards"
description: "Tracking the progress of the development of a custom mechanical keyboard from scratch."

remote_theme: pages-themes/modernist@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one